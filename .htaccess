<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /seu-projeto/  # Substitua se necessário, veja abaixo
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?page=$1 [QSA,L]
</IfModule>
